#!/usr/bin/env python3
"""geocode entry script."""

import sys
import os

# Add the project root to the path
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from geoffrey_llm.geocode.cli import main

if __name__ == "__main__":
    main()
